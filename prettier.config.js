/** @typedef {import("prettier").Config} Config */

/** @type {Config} */
const config = {
	arrowParens: "always",
	bracketSpacing: true,
	printWidth: 80,
	quoteProps: "preserve",
	semi: true,
	singleQuote: false,
	tabWidth: 2,
	useTabs: true,
};

export default config;
