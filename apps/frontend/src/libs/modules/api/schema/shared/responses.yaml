UnauthorizedError:
  description: Unauthorized
  content:
    application/json:
      schema:
        $ref: "#/schemas/Error"

NotFoundError:
  description: Resource not found
  content:
    application/json:
      schema:
        $ref: "#/schemas/Error"

BadRequestError:
  description: Bad request
  content:
    application/json:
      schema:
        $ref: "#/schemas/Error"

schemas:
  Error:
    type: object
    required:
      - errorType
      - message
    properties:
      message:
        type: string
      errorType:
        type: string

